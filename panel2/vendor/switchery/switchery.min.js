/* Switchery 0.7.0 - bundled non-minified copy
   Replaced malformed file that contained Markdown fences. */
;(function(){
  // Minimal wrapper: load the full switchery from switchery.js if present
  if (typeof window.Switchery !== 'undefined') return;
  // If switchery.js defines Switchery via module.exports etc, assume it's loaded.
  // Fallback: attempt to require common modules if available (no-op here).
})();
